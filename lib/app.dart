import 'package:flutter/material.dart';

class App extends StatelessWidget {
  final String flavor;

  const App({required this.flavor});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
